import AccountSettings from "@/components/Settings/AccountSettings"

export default function Settings() {
    return (
      <div className="h-screen flex flex-col p-8 sm:px-16 w-full">
        <AccountSettings />
      </div>
    );
  }